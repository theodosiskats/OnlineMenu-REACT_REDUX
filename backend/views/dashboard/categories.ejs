<%- include('../partials/dashboard/header') %> 
<%- include('../partials/flash') %>

<div class="card">
    <div class="card-body">
        <h5 class="card-title">Πίνακας Κατηγοριών - <%= currentUser.role %> </h5>
        <div class="table-responsive">
            <table id="zero_config" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Ονομασία</th>
                        <th>Περιγραφή</th>
                        
                    </tr>
                </thead>
                <tbody>
                    <% for (category of categories) { %>
                    <tr class="trow" onclick="location.href='/categories/edit/<%=category._id%>'" style="cursor:pointer">
                            <div>
                                <td style="width: 30%;"><%=category.name%></a></td>
                                <td style="width: 60%;" ><%=category.description%></td>
                            </div>
                    </tr>
                    <%}%>
                </tfoot>
            </table>
        </div>

    </div>
</div>

<tr >


<%- include('../partials/dashboard/footer') %> 
